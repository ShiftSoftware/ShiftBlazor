namespace ShiftSoftware.ShiftBlazor.Components;

/// <summary>
/// Soft-tinted banner pinned to a form's top, listing active attention signals with severity
/// icons, reasons, and timestamps. Supports explicit acknowledge (via the Acknowledge button)
/// and auto-clear-on-open modes. Integrated into <see cref="ShiftEntityForm{T}"/> automatically.
/// </summary>
public partial class AttentionBanner { }
