@inherits MudTimePicker

@namespace ShiftSoftware.ShiftBlazor.Components

@{
    if (this.For != null)
    {
        base.Required = FormHelper.IsRequired(this.For);
    }
    base.BuildRenderTree(__builder);
}
