<ShiftAutocomplete T="ShiftEntitySelectDTO"
                   TEntitySet="User"
                   EntitySet="Users"
                   DataValueField="@nameof(User.ID)"
                   DataTextField="@nameof(User.Name)"
                   Label="Autocomplete"
                   For="() => TheItem.Select"
                   Where="x => (user) => x != null"
                   >

</ShiftAutocomplete>

<br />
SelectedItem Text: @TheItem.Select?.Text

@code {
    User TheItem = new();

}